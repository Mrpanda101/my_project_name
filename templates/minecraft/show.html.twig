{% extends 'base.html.twig' %}

{% block title %}Minecraft{% endblock %}

{% block body %}
    <h1>Minecraft</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ minecraft.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ minecraft.Name }}</td>
            </tr>
            <tr>
                <th>Age</th>
                <td>{{ minecraft.Age }}</td>
            </tr>
            <tr>
                <th>Comment</th>
                <td>{{ minecraft.comment }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('minecraft_index') }}">back to list</a>

    <a href="{{ path('minecraft_edit', {'id': minecraft.id}) }}">edit</a>

    {{ include('minecraft/_delete_form.html.twig') }}
{% endblock %}
