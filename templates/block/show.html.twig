{% extends 'base.html.twig' %}

{% block title %}Block{% endblock %}

{% block body %}
    <h1>Block</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ block.id }}</td>
            </tr>
            <tr>
                <th>Blockname</th>
                <td>{{ block.blockname }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('block_index') }}">back to list</a>

    <a href="{{ path('block_edit', {'id': block.id}) }}">edit</a>

    {{ include('block/_delete_form.html.twig') }}
{% endblock %}
